export interface piaceData {

    type? : string;
    position? : string;
    color? : string;

    movedBefore? : boolean;
    canMove? :  boolean;

    pinned? : boolean;
    alive? : boolean;
    id? : string;
}